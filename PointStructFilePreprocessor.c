// Указатели
тип_данных *имя_указателя; // Объявление указателя на тип данных
имя_указателя = &имя_переменной; // Присваивание адреса переменной указателю
*имя_указателя // Разыменование указателя - получение значения по адресу
&имя_указателя // Получение адреса указателя
NULL // Специальное значение для указателя, означающее отсутствие адреса
sizeof(имя_указателя) // Получение размера указателя в байтах
// Пример работы с указателями
int x = 10; // Объявление и инициализация переменной типа int
int *p; // Объявление указателя на тип int
p = &x; // Присваивание адреса переменной x указателю p
printf("%d\n", *p); // Вывод значения по адресу, хранящемся в p (10)
*p = 20; // Изменение значения по адресу, хранящемся в p
printf("%d\n", x); // Вывод значения переменной x (20)

// Структуры
struct имя_структуры // Объявление структуры с именем
{
тип_данных имя_поля1; // Объявление полей структуры
тип_данных имя_поля2;
...
};
struct имя_структуры имя_переменной; // Объявление переменной типа структуры
имя_переменной.имя_поля // Доступ к полю структуры
struct имя_структуры *имя_указателя; // Объявление указателя на тип структуры
имя_указателя = &имя_переменной; // Присваивание адреса переменной типа структуры указателю
имя_указателя->имя_поля // Доступ к полю структуры по указателю
// Пример работы со структурами
struct point // Объявление структуры point
{
int x; // Объявление полей структуры
int y;
};
struct point p1; // Объявление переменной типа point
p1.x = 10; // Присваивание значения полю x
p1.y = 20; // Присваивание значения полю y
printf("%d %d\n", p1.x, p1.y); // Вывод значений полей структуры (10 20)
struct point *p2; // Объявление указателя на тип point
p2 = &p1; // Присваивание адреса переменной p1 указателю p2
p2->x = 30; // Изменение значения поля x по указателю
p2->y = 40; // Изменение значения поля y по указателю
printf("%d %d\n", p1.x, p1.y); // Вывод значений полей структуры (30 40)

// Файлы
FILE *имя_указателя; // Объявление указателя на тип FILE
имя_указателя = fopen("имя_файла", "режим_открытия"); // Открытие файла с заданным именем и режимом
fclose(имя_указателя); // Закрытие файла
fgetc(имя_указателя); // Чтение одного символа из файла
fputc(символ, имя_указателя); // Запись одного символа в файл
fgets(строка, размер, имя_указателя); // Чтение строки из файла
fputs(строка, имя_указателя); // Запись строки в файл
fread(буфер, размер_элемента, количество_элементов, имя_указателя); // Чтение блока байтов из файла
fwrite(буфер, размер_элемента, количество_элементов, имя_указателя); // Запись блока байтов в файл
fseek(имя_указателя, смещение, позиция); // Перемещение указателя в файле
ftell(имя_указателя); // Получение текущей позиции указателя в файле
rewind(имя_указателя); // Перемещение указателя в начало файла
feof(имя_указателя); // Проверка достижения конца файла
// Пример работы с файлами
FILE *fp; // Объявление указателя на тип FILE
fp = fopen("test.txt", "w"); // Открытие файла test.txt в режиме записи
if (fp == NULL) // Проверка успешности открытия файла
{
printf("Error opening file\n"); // Вывод сообщения об ошибке
return 1; // Завершение программы с кодом ошибки
}
fputs("Hello, file!", fp); // Запись строки в файл
fclose(fp); // Закрытие файла

// Препроцессор
#define имя_макроса замена // Определение макроса
#undef имя_макроса // Отмена определения макроса
#define имя_макроса(параметры) замена // Определение макроса с параметрами
#ifdef имя_макроса // Проверка, определен ли макрос
#ifndef имя_макроса // Проверка, не определен ли макрос
#if условие // Проверка, истинно ли условие
#else // Иначе
#endif // Конец условия
#include <имя_файла> // Включение содержимого файла
#include "имя_файла" // Включение содержимого файла
#error сообщение // Вывод сообщения об ошибке
#pragma команда // Выполнение специальной команды
// Пример работы с препроцессором
#define PI 3.14 // Определение макроса PI
#define SQUARE(x) ((x) * (x)) // Определение макроса SQUARE с параметром x
#include <stdio.h> // Включение содержимого файла stdio.h
int main()
{
printf("%f\n", PI); // Вывод значения макроса PI (3.14)
printf("%d\n", SQUARE(5)); // Вывод значения макроса SQUARE с аргументом 5 (25)
return 0;
}
